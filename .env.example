# Copy to .env and fill in. Never commit the real .env (already in .gitignore).

# Used by src/reasoning/rag_report.py to generate cited clinical narratives.
ANTHROPIC_API_KEY=

# Frontend build-time API base URL (frontend/.env picks this up via Vite).
VITE_API_BASE_URL=http://localhost:8000
