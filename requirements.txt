# --- Core & Math ---
numpy>=1.24.0,<2.0.0
pandas>=2.0.0
scipy>=1.10.0
pyyaml>=6.0

# --- PyTorch & Deep Learning ---
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0
pytorch-lightning>=2.0.0
timm>=0.9.2
transformers>=4.35.0

# --- Audio & Vision Processing ---
librosa>=0.10.0
soundfile>=0.12.1
torchcodec>=0.16.0
opencv-python>=4.7.0
Pillow>=9.5.0
albumentations>=1.3.0

# --- Tabular & Classic ML ---
scikit-learn>=1.2.2
lightgbm>=3.3.5
xgboost>=1.7.5
imbalanced-learn>=0.10.1
pytorch-tabular>=1.0.2

# --- Explainability (XAI) & Calibration ---
shap>=0.42.1
captum>=0.6.0
dice-ml>=0.9
mapie>=1.0.0

# --- RAG, Vector Search & LLM ---
# The reasoning stack runs locally on Ollama: `llama3.1` writes the cited
# narratives, `nomic-embed-text` embeds the clinical KB. Install Ollama
# separately (https://ollama.com), then:
#     ollama pull llama3.1
#     ollama pull nomic-embed-text
# If Ollama is unreachable the API degrades to a templated report and TF-IDF
# retrieval rather than failing -- see src/reasoning/ollama_config.py.
langchain-ollama>=0.2.0
langgraph>=0.0.30
faiss-cpu>=1.7.4
sentence-transformers>=2.2.2   # fallback embedding backend
anthropic>=0.18.0              # optional hosted-report generator

# NOTE: `langchain` and `langchain-community` are deliberately NOT pinned here.
# Nothing under src/ imports them (only `langgraph` and `langchain_ollama` are
# used), and keeping them installed forces langchain-core back to <1.0, which
# conflicts with the version langchain-ollama requires.

# --- API & Serving ---
fastapi>=0.100.0
uvicorn>=0.22.0
pydantic>=2.0.0
python-multipart>=0.0.6
onnxruntime>=1.16.0

# --- MLOps & Utilities ---
tqdm>=4.65.0
matplotlib>=3.7.1
seaborn>=0.12.2
python-dotenv>=1.0.0
pytest>=7.4.0
httpx>=0.24.0
hydra-core>=1.3.2
omegaconf>=2.3.0
